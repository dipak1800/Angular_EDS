import { CustomAgePipe } from './custom-age.pipe';

describe('CustomAgePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomAgePipe();
    expect(pipe).toBeTruthy();
  });
});
